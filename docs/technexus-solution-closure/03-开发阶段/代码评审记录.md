# 代码评审记录

文档编号：`DEV-CR-001`  
版本：`v0.5`  
状态：`通过/待用户确认`  
评审对象：`DEV-BUILD-20260915-05`  
源分支/目标分支：工作区 `main` → 待用户确认后建立 `codex/technexus-alpha`  
提交范围：HEAD `163f8cd` + 当前工作区  
评审人：技术负责人、安全、数据库、测试专家视角

## 1. 本轮结论

第 5 轮关闭了阶段三剩余质量门禁。最终代码维持用户确认的 12 个工程名、`com.technexus` 根包和 `tn_` 单数表名，并保持 Java 21 + Spring Boot 3.5 + DDD + 端口/适配器的模块化单体结构。

真实 MySQL 8.4.11 下 11 个 Flyway migration、JDBC 事务、HTTP 幂等、共享登录限流及 Refresh 并发均验证通过；94/94 Java 测试通过。JaCoCo 总行覆盖率为 77.1%，Domain 分支覆盖率为 90.1%。双前端 lint/build/audit、Gitleaks 及基于 CycloneDX SBOM 的 OSV 扫描均通过，最终扫描 105 个后端组件、779 个用户端包和 223 个管理端包，未发现问题。

## 2. 关键改进

| 范围 | 结果 |
|---|---|
| API 契约与幂等 | 校验 OpenAPI 3.1 的 29 paths、35 operationId、Schema、状态码和写请求幂等头；MySQL 持久化幂等支持重放、冲突和处理中状态 |
| Auth | MySQL 原子共享限流；真实并发 Refresh 仅一次成功，重用撤销令牌族 |
| 审核/定价 | Content/Demand 提交、AuditTask、人工决定、目标版本推进和 FREE 定价发布保持同事务规则 |
| 文件 | S3 预签名与 ClamAV Worker 支持租约、重试、污染隔离和失败关闭 |
| AI | `QUEUED→RUNNING→SUCCEEDED/FAILED` Worker、超时/重试和人工降级可测试 |
| 数据库 | V1—V11 连续迁移在 MySQL 8.4.11 migrate/validate；所有表遵循 `tn_` 小写 snake_case 单数命名 |
| 供应链 | Spring Boot 3.5.16、Jetty、修复版本依赖；CycloneDX 2.9.3 + OSV 2.6.0 与 Gitleaks CI 阻断 |
| CI/前端 | Maven+MySQL、覆盖率、SBOM/OSV、依赖评审、密钥扫描、双前端 ESLint/build/npm audit 全部自动阻断 |

## 3. 自动检查结果

| 检查 | 结果 | 阈值/证据 |
|---|---|---|
| Maven `spotless:apply clean install` | 通过 | 11 模块成功；94/94，0 失败、0 跳过 |
| MySQL 集成 | 通过 | MySQL 8.4.11；11 migrations；3 个真实数据库集成测试 |
| JaCoCo 总行覆盖率 | 通过 | 77.1%（2157/2796）≥70% |
| Domain 分支覆盖率 | 通过 | 90.1%（209/232）≥80% |
| ArchUnit/命名 | 通过 | DDD 依赖 0 违规；`tn_` 和 V1—V11 连续命名 |
| API 契约 | 通过 | 29 paths、35 operationId；Schema/状态码/幂等要求一致 |
| 双前端 | 通过 | ESLint 0 warning；Nuxt、vue-tsc/Vite 生产构建成功 |
| npm audit | 通过 | 两个生产依赖树均 0 vulnerability |
| OSV | 通过 | 105 + 779 + 223 个组件/包；No issues found |
| Gitleaks | 通过 | 排除生成目录后源码、配置和锁文件无密钥泄漏 |

## 4. 架构与 PRD 复核

- 8 个业务模块分别承载 Auth、User、Community、Content、Demand、Audit、Pricing、File 限界上下文；领域层不依赖 Spring、JDBC 或 Web，Controller 不承载业务规则。
- MySQL 是生产唯一业务事实源，`InMemory*` 仅由测试 profile 或测试夹具装配。
- 内容与需求审核、免费定价发布、文件状态、Refresh 轮换、AI 建议均由显式状态机或应用事务推进，不返回伪成功。
- OpenAPI 与 Controller 的成功状态码、JSON Schema、登录字段和幂等要求已逐项自动对照。
- 未发现与 PRD V4.0、阶段一 v1.0 需求、阶段二 v1.0 设计或用户命名规范不符的新问题。

## 5. 问题关闭

| 编号 | 状态 | 关闭证据 |
|---|---|---|
| DEV-ISSUE-001/010 | 已关闭 | Admin JDBC 与审核跨上下文事务在真实 MySQL 验证 |
| DEV-ISSUE-002 | 实现已关闭 | ClamAV 协议、Worker、重试/隔离单测；真实 daemon 联调列入阶段四 |
| DEV-ISSUE-003 | 已关闭 | 完整 OpenAPI 契约测试和 MySQL HTTP 幂等 |
| DEV-ISSUE-004/007 | 已关闭 | MySQL 8.4.11 V1—V11、完整 JDBC 和并发 Refresh |
| DEV-ISSUE-005 | 已关闭 | 77.1% 总行、90.1% Domain 分支，脚本/CI 阻断 |
| DEV-ISSUE-006 | 已关闭 | Spotless、ESLint、构建、SCA、密钥扫描 CI |
| DEV-ISSUE-008 | 已关闭 | MySQL 共享原子登录限流及滥用测试 |
| DEV-ISSUE-011 | 已关闭 | AI Worker 成功、重试、最终失败测试 |
| DEV-ISSUE-009 | 待用户确认后执行 | 按工作流不得在阶段门禁确认前建立提交/PR 基线，不作为代码红线 |

## 6. 遗留到阶段四的验证

真实 S3/ClamAV 联调、性能/容量、可访问性、渗透、目标服务器硬件、异地备份和恢复演练属于阶段四测试或上线前红线。本轮已有适配器与失败关闭单测，故不构成阶段三代码门禁失败，但不代表可直接生产上线。

## 7. 评审结果

- 未关闭严重问题：0。
- 质量门禁：全部通过。
- 阶段评分：94/100。
- 合并结论：阶段三代码基线通过，等待用户确认；确认前不创建阶段四目录、不提交或发布。

## 8. 变更记录

| 日期 | 版本 | 说明 |
|---|---|---|
| 2026-09-14 | v0.1 | 首轮评审：29/29，记录 2 个严重问题 |
| 2026-09-14 | v0.2 | 第二轮：47/47，核心路径迁移到 DDD/MySQL，得分 72 |
| 2026-09-15 | v0.3 | 第三轮：Admin JDBC、文件扫描、52/52，发现审核编排红线 |
| 2026-09-15 | v0.4 | 第四轮：审核/定价闭环、56/56，得分 84 |
| 2026-09-15 | v0.5 | 第五轮：94/94、MySQL、覆盖率、契约、AI、CI、SCA 全部通过，得分 94 |
